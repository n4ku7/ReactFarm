# Database branch helper

This branch contains database-related notes and any staging data used for migrations or seeding.

Do not store production credentials here. Use `.env` and ensure secrets are in CI/secret storage.

Contents:
- `.gitkeep` placeholder (keeps folder in repo)

Usage:
- Add migration scripts or seed JSON files here if needed for review before merging to `main`.
